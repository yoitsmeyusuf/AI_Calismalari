# Referans: bu ortamda (.venv) zaten kurulu olan sürümler.
# Yeniden kurmak gerekirse: uv pip install -r requirements.txt
torch==2.10.0
transformers==5.5.0
datasets==4.3.0
tokenizers==0.22.2
trl==0.24.0
peft==0.19.1
unsloth==2026.7.2
unsloth_zoo==2026.7.2
huggingface_hub==1.23.0
accelerate==1.14.0
bitsandbytes==0.49.2
python-dotenv==1.2.2
hf_transfer==0.1.9
playwright==1.61.0
beautifulsoup4==4.15.0
requests==2.34.2

# playwright kurulumdan sonra tarayıcı binary'sini de indirmeniz gerekir:
#   .venv/bin/python -m playwright install chromium

# 7. ve 8. hafta (tool calling + Gradio) kendi requirements dosyalarını kullanıyor;
# bu dosyaya eklenmedi çünkü Space'te torch/unsloth gerekmiyor. Yerelde denemek için:
#   uv pip install --python .venv/bin/python -r hafta7_tool_calling/requirements.txt
#   uv pip install --python .venv/bin/python -r hafta8_veritabani_ajani/requirements.txt
# 8. haftanın veritabanı için ek paket yok: sqlite3 standart kütüphanede.
